# Real customer + harvested concern data — MUST NOT be distributed in the repo.
# Contains real Tekmetric RO concern text (potential PII: names/phones/VINs) and
# forum-harvested text. Untracked 2026-07-19 (Chris's data-governance decision after
# the GPT/Gemini cross-verify). Keep these locally to run the unbiased eval; they are
# regenerated by label-real-concerns.ts from a source pull. NEVER re-commit them.
real-concerns-*.json

# Eval run OUTPUTS that embed the real customer text in their per-row `text` field.
# Regenerable via run-eval-final.ts; not source of truth. Excluded for the same reason.
final-baseline*.json
final-s3-*.json
final-current*.json
act-or-ask*-report.json
cross-judge-report.json
stage3-adjudication.json

# Human spot-check artifacts — embed real customer text + Chris's verdicts. Local only.
spot-check-sheet.json
spot-check.html
spot-check-filled.json
