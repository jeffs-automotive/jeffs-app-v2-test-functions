# LLM diagnostic test — batch 1 (May 2026)

**Ran:** 2026-05-20T22:34:42.459Z
**Model:** `claude-haiku-4-5`
**Catalog at test time:** 15 active testing services + 6 'other' subcategories = 21 entries
**Chip hint:** Other Issue (no pre-classification — the hardest classification case for the LLM)
**Endpoint:** `https://itzdasxobllfiuolmbxu.supabase.co/functions/v1/llm-testing`

## Per-step labels

- `matched 'X'` — successful step
- `LLM returned null` — LLM intentionally declined (not a failure)
- `hallucinated` — LLM returned a slug not in the catalog; post-validation dropped it
- `silently_failed` — values dropped by validation without an explicit error (e.g. question IDs not in the matched subcategory)
- `failed` — LLM call itself errored or returned malformed structured output
- `short_circuit` — pre-LLM short-circuit (desc<3 chars)
- `skipped` — upstream step's outcome made this step a no-op

## Test cases

### Test 1

```
{
concern: My car makes a loud knocking noise when I first start it in the morning but it stops after about a minute
LLM decision tree:
  step 1 (match category):       failed — LLM call errored: No object generated: response did not match schema.
  step 2 (vagueness check):      passed — description has 105 chars (>=3)
  step 3 (pick subcategory):     skipped — no matched category from step 1
  step 4 (gap-detect questions): skipped — no valid subcategory from step 3
  step 5 (generate reasoning):   skipped — no LLM output
matched category key: null
matched sub category slug: null
unanswered question ids: []
testing service recommended: none (forwarded to advisor)
}
```

<sub>Latency: 2357ms (wall 2892ms) · Tokens in/out: 0/0 · err: No object generated: response did not match schema.</sub>

### Test 2

```
{
concern: Brakes squeak really bad when I'm coming to a stop especially at slow speeds
LLM decision tree:
  step 1 (match category):       matched 'brake_inspection'
  step 2 (vagueness check):      passed — description has 76 chars (>=3)
  step 3 (pick subcategory):     matched 'high_pitched_squealing'
  step 4 (gap-detect questions): 4 unanswered IDs (all valid)
  step 5 (generate reasoning):   "Customer reports high-pitched squealing from brakes when coming to a stop at slow speeds, matching the high_pitched_squealing brake noise subcategory under brake_inspection testing service."
matched category key: brake_inspection
matched sub category slug: high_pitched_squealing
unanswered question ids: [624, 627, 628, 629]
testing service recommended: brake_inspection — Brake inspection ($39.99)
}
```

<sub>Latency: 1960ms (wall 2631ms) · Tokens in/out: 38508/147</sub>

### Test 3

```
{
concern: AC blows hot air on the driver side but cold on the passenger side
LLM decision tree:
  step 1 (match category):       failed — LLM call errored: No object generated: response did not match schema.
  step 2 (vagueness check):      passed — description has 66 chars (>=3)
  step 3 (pick subcategory):     skipped — no matched category from step 1
  step 4 (gap-detect questions): skipped — no valid subcategory from step 3
  step 5 (generate reasoning):   skipped — no LLM output
matched category key: null
matched sub category slug: null
unanswered question ids: []
testing service recommended: none (forwarded to advisor)
}
```

<sub>Latency: 2732ms (wall 3265ms) · Tokens in/out: 0/0 · err: No object generated: response did not match schema.</sub>

### Test 4

```
{
concern: When I turn the steering wheel all the way left I hear a clicking noise
LLM decision tree:
  step 1 (match category):       matched 'suspension_check'
  step 2 (vagueness check):      passed — description has 71 chars (>=3)
  step 3 (pick subcategory):     matched 'popping_or_clicking_when_turning'
  step 4 (gap-detect questions): 7 unanswered IDs (all valid)
  step 5 (generate reasoning):   "Customer reports clicking noise when turning the steering wheel all the way left, which matches the "popping or clicking when turning" subcategory under suspension_check service."
matched category key: suspension_check
matched sub category slug: popping_or_clicking_when_turning
unanswered question ids: [113, 114, 115, 116, 117, 118, 119]
testing service recommended: suspension_check — Suspension check ($89.95)
}
```

<sub>Latency: 1550ms (wall 2030ms) · Tokens in/out: 38505/155</sub>

### Test 5

```
{
concern: Battery keeps dying overnight even though it's only 2 years old
LLM decision tree:
  step 1 (match category):       matched 'battery_test'
  step 2 (vagueness check):      passed — description has 63 chars (>=3)
  step 3 (pick subcategory):     matched 'battery_drains_overnight'
  step 4 (gap-detect questions): 6 unanswered IDs (all valid)
  step 5 (generate reasoning):   "Battery drains overnight subcategory matches the customer's symptom that their 2-year-old battery dies overnight; battery_test service provides free complete electrical-system diagnostics to identify the root cause."
matched category key: battery_test
matched sub category slug: battery_drains_overnight
unanswered question ids: [532, 533, 534, 535, 536, 538]
testing service recommended: battery_test — Battery test ($0.00)
}
```

<sub>Latency: 1901ms (wall 2383ms) · Tokens in/out: 38504/157</sub>

### Test 6

```
{
concern: There's a sweet syrupy smell coming from under the hood after driving for a while
LLM decision tree:
  step 1 (match category):       matched 'check_ac'
  step 2 (vagueness check):      passed — description has 81 chars (>=3)
  step 3 (pick subcategory):     hallucinated — LLM returned subcategory 'sweet_smell_maple_syrup_antifreeze' which is NOT in the matched category's eligible set
  step 4 (gap-detect questions): skipped — no valid subcategory from step 3
  step 5 (generate reasoning):   "Sweet smell from under the hood after driving suggests a coolant leak; customer describes the classic maple-syrup antifreeze odor, triggering the check_ac service's cooling-system diagnostic capability under the 'sweet_smell_maple_syrup_antifreeze' subcategory."
matched category key: check_ac
matched sub category slug: null
unanswered question ids: []
testing service recommended: check_ac — A/C performance check ($89.95)
}
```

<sub>Latency: 2262ms (wall 2478ms) · Tokens in/out: 38508/182</sub>

### Test 7

```
{
concern: White smoke coming out the tailpipe when I accelerate hard
LLM decision tree:
  step 1 (match category):       matched 'coolant_leak_testing'
  step 2 (vagueness check):      passed — description has 58 chars (>=3)
  step 3 (pick subcategory):     matched 'white_smoke_from_tailpipe'
  step 4 (gap-detect questions): 7 unanswered IDs (all valid)
  step 5 (generate reasoning):   "White smoke from tailpipe often signals coolant burning in the engine (head gasket or internal leak); matched to coolant_leak_testing service and white_smoke_from_tailpipe subcategory, with customer describing smoke during hard acceleration."
matched category key: coolant_leak_testing
matched sub category slug: white_smoke_from_tailpipe
unanswered question ids: [281, 282, 283, 284, 285, 286, 287]
testing service recommended: coolant_leak_testing — Coolant leak / overheating testing ($109.95)
}
```

<sub>Latency: 2145ms (wall 2491ms) · Tokens in/out: 38503/176</sub>

### Test 8

```
{
concern: Brake pedal goes almost to the floor before the brakes engage
LLM decision tree:
  step 1 (match category):       failed — LLM call errored: No object generated: response did not match schema.
  step 2 (vagueness check):      passed — description has 61 chars (>=3)
  step 3 (pick subcategory):     skipped — no matched category from step 1
  step 4 (gap-detect questions): skipped — no valid subcategory from step 3
  step 5 (generate reasoning):   skipped — no LLM output
matched category key: null
matched sub category slug: null
unanswered question ids: []
testing service recommended: none (forwarded to advisor)
}
```

<sub>Latency: 2252ms (wall 2685ms) · Tokens in/out: 0/0 · err: No object generated: response did not match schema.</sub>

### Test 9

```
{
concern: Steering wheel pulls hard to the right whenever I let go on the highway
LLM decision tree:
  step 1 (match category):       matched 'suspension_check'
  step 2 (vagueness check):      passed — description has 71 chars (>=3)
  step 3 (pick subcategory):     matched 'pulling_drifting_or_wandering_on_the_road'
  step 4 (gap-detect questions): 6 unanswered IDs (all valid)
  step 5 (generate reasoning):   "Customer reports steering wheel pulling hard to the right on highway, matching the pulling_drifting_or_wandering_on_the_road subcategory under suspension_check service; described steady directional pull while cruising."
matched category key: suspension_check
matched sub category slug: pulling_drifting_or_wandering_on_the_road
unanswered question ids: [696, 697, 698, 699, 700, 701]
testing service recommended: suspension_check — Suspension check ($89.95)
}
```

<sub>Latency: 2241ms (wall 2629ms) · Tokens in/out: 38505/172</sub>

### Test 10

```
{
concern: Tires wearing unevenly on the front passenger side, looks like cupping
LLM decision tree:
  step 1 (match category):       matched 'suspension_check'
  step 2 (vagueness check):      passed — description has 70 chars (>=3)
  step 3 (pick subcategory):     hallucinated — LLM returned subcategory 'uneven_tire_wear_bald_spots' which is NOT in the matched category's eligible set
  step 4 (gap-detect questions): skipped — no valid subcategory from step 3
  step 5 (generate reasoning):   "Uneven tire wear with cupping pattern on front passenger side suggests suspension or alignment issues; suspension_check service can inspect suspension components, bushings, and alignment-related wear that causes this cupping symptom."
matched category key: suspension_check
matched sub category slug: null
unanswered question ids: []
testing service recommended: suspension_check — Suspension check ($89.95)
}
```

<sub>Latency: 2548ms (wall 2961ms) · Tokens in/out: 38507/160</sub>

### Test 11

```
{
concern: Check engine light came on yesterday but car drives normal
LLM decision tree:
  step 1 (match category):       matched 'warning_light_general'
  step 2 (vagueness check):      passed — description has 58 chars (>=3)
  step 3 (pick subcategory):     matched 'check_engine_light'
  step 4 (gap-detect questions): 7 unanswered IDs (all valid)
  step 5 (generate reasoning):   "Customer describes a check engine light that appeared yesterday with no drivability symptoms, matching the check_engine_light subcategory under warning_light_general testing service."
matched category key: warning_light_general
matched sub category slug: check_engine_light
unanswered question ids: [372, 373, 374, 375, 376, 377, 378]
testing service recommended: warning_light_general — Warning light testing (non-TPMS) ($179.99)
}
```

<sub>Latency: 1799ms (wall 2406ms) · Tokens in/out: 38500/153</sub>

### Test 12

```
{
concern: ABS light just turned on a few minutes ago and stayed on
LLM decision tree:
  step 1 (match category):       matched 'alternator_testing'
  step 2 (vagueness check):      passed — description has 56 chars (>=3)
  step 3 (pick subcategory):     matched 'abs_anti_lock_brake_light'
  step 4 (gap-detect questions): 7 unanswered IDs (all valid)
  step 5 (generate reasoning):   "ABS light illuminated matches the abs_anti_lock_brake_light subcategory under warning_light concern category; customer's brief description lacks detail on braking feel, timing of onset, recent service, or water exposure."
matched category key: alternator_testing
matched sub category slug: abs_anti_lock_brake_light
unanswered question ids: [413, 414, 415, 416, 417, 418, 419]
testing service recommended: alternator_testing — Alternator testing (simple electrical) ($89.95)
}
```

<sub>Latency: 2079ms (wall 2812ms) · Tokens in/out: 38503/169</sub>

### Test 13

```
{
concern: Airbag light is flashing intermittently
LLM decision tree:
  step 1 (match category):       matched 'warning_light_general'
  step 2 (vagueness check):      passed — description has 39 chars (>=3)
  step 3 (pick subcategory):     matched 'airbag_srs_light'
  step 4 (gap-detect questions): 6 unanswered IDs (all valid)
  step 5 (generate reasoning):   "Airbag/SRS warning light with intermittent flashing pattern matches the airbag_srs_light subcategory, eligible under warning_light_general testing service; customer's description identifies the light type and its flashing behavior."
matched category key: warning_light_general
matched sub category slug: airbag_srs_light
unanswered question ids: [427, 428, 429, 430, 432, 433]
testing service recommended: warning_light_general — Warning light testing (non-TPMS) ($179.99)
}
```

<sub>Latency: 2001ms (wall 2392ms) · Tokens in/out: 38501/171</sub>

### Test 14

```
{
concern: Oil pressure light flickers when I come to a stop at idle
LLM decision tree:
  step 1 (match category):       matched 'alternator_testing'
  step 2 (vagueness check):      passed — description has 57 chars (>=3)
  step 3 (pick subcategory):     matched 'oil_pressure_light'
  step 4 (gap-detect questions): 5 unanswered IDs (all valid)
  step 5 (generate reasoning):   "Oil pressure light flickering at idle maps to the oil_pressure_light subcategory; alternator testing is the diagnostic service for warning lights, which tests the charging system that can cause electrical warning-light glitches when voltage is unstable."
matched category key: alternator_testing
matched sub category slug: oil_pressure_light
unanswered question ids: [393, 394, 395, 397, 398]
testing service recommended: alternator_testing — Alternator testing (simple electrical) ($89.95)
}
```

<sub>Latency: 1839ms (wall 2401ms) · Tokens in/out: 38503/162</sub>

### Test 15

```
{
concern: Car has been sitting in my driveway for 8 months, want to make sure it's road ready before driving it
LLM decision tree:
  step 1 (match category):       matched 'car_has_been_sitting_unused_for_a_long_time'
  step 2 (vagueness check):      passed — description has 101 chars (>=3)
  step 3 (pick subcategory):     matched 'car_has_been_sitting_unused_for_a_long_time'
  step 4 (gap-detect questions): 6 unanswered IDs (all valid)
  step 5 (generate reasoning):   "Customer describes a car that has been sitting in the driveway for 8 months and wants to verify it's road-ready before driving, which matches the "car has been sitting unused for a long time" elevated subcategory."
matched category key: car_has_been_sitting_unused_for_a_long_time
matched sub category slug: car_has_been_sitting_unused_for_a_long_time
unanswered question ids: [794, 795, 796, 797, 798, 799]
testing service recommended: none (forwarded to advisor)
}
```

<sub>Latency: 1851ms (wall 2437ms) · Tokens in/out: 38516/188</sub>

### Test 16

```
{
concern: Just got rear-ended last week and now the car pulls left, want to make sure suspension is OK
LLM decision tree:
  step 1 (match category):       matched 'after_a_recent_accident_or_impact'
  step 2 (vagueness check):      passed — description has 92 chars (>=3)
  step 3 (pick subcategory):     matched 'after_a_recent_accident_or_impact'
  step 4 (gap-detect questions): 5 unanswered IDs (all valid)
  step 5 (generate reasoning):   "Recent rear-end accident with subsequent pulling symptom suggests post-impact damage assessment, not a standalone suspension or pulling issue—the 'after recent accident' other subcategory is the appropriate route."
matched category key: after_a_recent_accident_or_impact
matched sub category slug: after_a_recent_accident_or_impact
unanswered question ids: [766, 767, 768, 770, 771]
testing service recommended: none (forwarded to advisor)
}
```

<sub>Latency: 2060ms (wall 2483ms) · Tokens in/out: 38511/165</sub>

### Test 17

```
{
concern: Going on a 1500 mile road trip next weekend, want a complete check before I go
LLM decision tree:
  step 1 (match category):       matched 'general_check_up_or_pre_trip_inspection'
  step 2 (vagueness check):      passed — description has 78 chars (>=3)
  step 3 (pick subcategory):     matched 'general_check_up_or_pre_trip_inspection'
  step 4 (gap-detect questions): 6 unanswered IDs (all valid)
  step 5 (generate reasoning):   "Customer explicitly stated they are preparing for a long road trip next weekend and want a complete check before departure, matching the pre-trip inspection category directly."
matched category key: general_check_up_or_pre_trip_inspection
matched sub category slug: general_check_up_or_pre_trip_inspection
unanswered question ids: [787, 788, 789, 790, 791, 792]
testing service recommended: none (forwarded to advisor)
}
```

<sub>Latency: 6710ms (wall 7265ms) · Tokens in/out: 38510/161</sub>

### Test 18

```
{
concern: Just had new tires installed at Discount Tire yesterday and now I feel a vibration at 65mph
LLM decision tree:
  step 1 (match category):       matched 'tpms_testing'
  step 2 (vagueness check):      passed — description has 91 chars (>=3)
  step 3 (pick subcategory):     matched 'recent_tire_work_then_new_symptom'
  step 4 (gap-detect questions): 5 unanswered IDs (all valid)
  step 5 (generate reasoning):   "Customer reports vibration at highway speed immediately after new tire installation yesterday, matching recent_tire_work_then_new_symptom which handles new symptoms following tire work."
matched category key: tpms_testing
matched sub category slug: recent_tire_work_then_new_symptom
unanswered question ids: [752, 754, 755, 756, 757]
testing service recommended: tpms_testing — Tire pressure (TPMS) light testing ($54.95)
}
```

<sub>Latency: 1792ms (wall 2387ms) · Tokens in/out: 38513/154</sub>

### Test 19

```
{
concern: Engine bay smells like burning oil after I drive for like 20 minutes
LLM decision tree:
  step 1 (match category):       matched 'oil_leak_testing'
  step 2 (vagueness check):      passed — description has 68 chars (>=3)
  step 3 (pick subcategory):     matched 'burnt_oil_smell'
  step 4 (gap-detect questions): 7 unanswered IDs (all valid)
  step 5 (generate reasoning):   "Burnt oil smell subcategory under oil_leak_testing service; customer reports burning oil smell from engine bay after extended driving, indicating potential leak or oil management issue."
matched category key: oil_leak_testing
matched sub category slug: burnt_oil_smell
unanswered question ids: [232, 233, 234, 235, 236, 237, 238]
testing service recommended: oil_leak_testing — Oil leak testing ($179.95)
}
```

<sub>Latency: 1687ms (wall 2483ms) · Tokens in/out: 38506/153</sub>

### Test 20

```
{
concern: Squealing high-pitched noise from the front right wheel when I brake but only sometimes
LLM decision tree:
  step 1 (match category):       matched 'brake_inspection'
  step 2 (vagueness check):      passed — description has 87 chars (>=3)
  step 3 (pick subcategory):     matched 'high_pitched_squealing'
  step 4 (gap-detect questions): silently_failed — LLM returned 6 IDs but 1 weren't in matched subcategory; 5 kept
  step 5 (generate reasoning):   "High-pitched squealing during braking from the front right wheel matches brake_inspection's high_pitched_squealing subcategory; location and braking trigger are explicit in the description but frequency inconsistency and other contextual details remain unaddressed."
matched category key: brake_inspection
matched sub category slug: high_pitched_squealing
unanswered question ids: [624, 626, 627, 628, 839]
testing service recommended: brake_inspection — Brake inspection ($39.99)
}
```

<sub>Latency: 2114ms (wall 2672ms) · Tokens in/out: 38508/167</sub>

### Test 21

```
{
concern: Something just feels off, can't really describe it
LLM decision tree:
  step 1 (match category):       LLM returned null — declined to categorize
  step 2 (vagueness check):      passed — description has 50 chars (>=3)
  step 3 (pick subcategory):     skipped — no matched category from step 1
  step 4 (gap-detect questions): skipped — no valid subcategory from step 3
  step 5 (generate reasoning):   "Description is too vague ("something just feels off, can't really describe it") with no specific symptom details to match against any category or subcategory."
matched category key: null
matched sub category slug: null
unanswered question ids: []
testing service recommended: none (forwarded to advisor)
}
```

<sub>Latency: 6708ms (wall 7164ms) · Tokens in/out: 38500/123</sub>

### Test 22

```
{
concern: Car shakes when braking at highway speeds AND the check engine light is on AND it pulls left
LLM decision tree:
  step 1 (match category):       failed — LLM call errored: No object generated: response did not match schema.
  step 2 (vagueness check):      passed — description has 92 chars (>=3)
  step 3 (pick subcategory):     skipped — no matched category from step 1
  step 4 (gap-detect questions): skipped — no valid subcategory from step 3
  step 5 (generate reasoning):   skipped — no LLM output
matched category key: null
matched sub category slug: null
unanswered question ids: []
testing service recommended: none (forwarded to advisor)
}
```

<sub>Latency: 2128ms (wall 2754ms) · Tokens in/out: 0/0 · err: No object generated: response did not match schema.</sub>

### Test 23

```
{
concern: It's making a weird noise
LLM decision tree:
  step 1 (match category):       LLM returned null — declined to categorize
  step 2 (vagueness check):      passed — description has 25 chars (>=3)
  step 3 (pick subcategory):     skipped — no matched category from step 1
  step 4 (gap-detect questions): skipped — no valid subcategory from step 3
  step 5 (generate reasoning):   "Description is too vague to categorize — "weird noise" alone does not specify the type, location, or context needed to match any of the 14 testing services or 6 'other' subcategories."
matched category key: null
matched sub category slug: null
unanswered question ids: []
testing service recommended: none (forwarded to advisor)
}
```

<sub>Latency: 2381ms (wall 2907ms) · Tokens in/out: 38496/136</sub>

### Test 24

```
{
concern: I think my transmission is slipping but I'm not really sure
LLM decision tree:
  step 1 (match category):       matched 'transmission_testing'
  step 2 (vagueness check):      passed — description has 59 chars (>=3)
  step 3 (pick subcategory):     matched 'low_power_or_wont_accelerate_normally'
  step 4 (gap-detect questions): 7 unanswered IDs (all valid)
  step 5 (generate reasoning):   "Customer reports possible transmission slipping, which maps to transmission_testing service; "low power or won't accelerate normally" is the eligible subcategory best matching vague slip symptoms, as transmission slip typically manifests as power loss or lag under acceleration."
matched category key: transmission_testing
matched sub category slug: low_power_or_wont_accelerate_normally
unanswered question ids: [1182, 1183, 1184, 1185, 1186, 1187, 1188]
testing service recommended: transmission_testing — Transmission issues testing ($179.95)
}
```

<sub>Latency: 2131ms (wall 2514ms) · Tokens in/out: 38503/183</sub>

### Test 25

```
{
concern: The car just isn't right anymore
LLM decision tree:
  step 1 (match category):       LLM returned null — declined to categorize
  step 2 (vagueness check):      passed — description has 32 chars (>=3)
  step 3 (pick subcategory):     skipped — no matched category from step 1
  step 4 (gap-detect questions): skipped — no valid subcategory from step 3
  step 5 (generate reasoning):   "The description "The car just isn't right anymore" is too vague to categorize; it lacks specific symptoms (noise, performance issue, warning light, leak, etc.) and does not map to any subcategory in the catalog."
matched category key: null
matched sub category slug: null
unanswered question ids: []
testing service recommended: none (forwarded to advisor)
}
```

<sub>Latency: 1796ms (wall 2354ms) · Tokens in/out: 38497/139</sub>

## Batch summary

| metric | value |
|---|---|
| total concerns | 25 |
| matched a testing service | 15 |
| matched an 'other' subcategory (forward-to-advisor) | 3 |
| null match (forwarded to advisor) | 7 |
| hallucinated category (step 1) | 0 |
| hallucinated subcategory (step 3) | 2 |
| silently filtered question IDs (step 4) | 1 |
| LLM call failed (step 1 failed) | 4 |
| short-circuit triggered (step 2) | 0 |
| sum of LLM latencies | 61024 ms |
| sum of tokens (in + out) | 811980 |
